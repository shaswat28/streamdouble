__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.wav
!fixtures/*.wav
!tests/data/*.wav
.DS_Store
fixtures/speech/

# Demo scratch: regenerated by DEMO.md, not a build artifact.
demo-out/

# Traces written by documented commands. tests/test_documented_commands.py runs
# every command in the README from the repository root, so the --trace example
# lands one here on every test run.
*.jsonl
